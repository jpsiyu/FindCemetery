<!-- 
<view class='page'>
  <view class='cemetery-container'>
    <view class='cemetery' wx:for="{{cemetery}}" wx:key="index" style='height:{{gridHeight}}px'>
      {{item.name}}
      {{item.locationName}}
      {{item.location}}
    </view>
  </view>
</view>
-->
<div class='container'>
  <ul class="list-group">
    <li class="list-group-item" wx:for="{{cemetery}}" wx:key="index">{{item.name}} </li>
  </ul>
</div>